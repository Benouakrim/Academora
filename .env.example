# ---- Core API ----
PORT=3001
API_BASE_URL=http://localhost:3001
FRONTEND_URL=http://localhost:5173
NODE_ENV=development

# ---- Auth ----
JWT_SECRET=replace-with-a-long-random-string

# ---- OAuth (Google) ----
# Create credentials in Google Cloud Console > APIs & Services > Credentials
# Authorized JavaScript origin (local): http://localhost:5173
# Authorized redirect URI (local): http://localhost:3001/api/auth/oauth/google/callback
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_OAUTH_ENABLED=true

# ---- Supabase ----
SUPABASE_URL=your-supabase-url
# Use service role on the server side
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key
# Optionally allow using SUPABASE_KEY for local dev
SUPABASE_KEY=

# ---- Mail (Password reset, notifications) ----
MAILJET_API_KEY=
MAILJET_API_SECRET=
MAILJET_SENDER_EMAIL=no-reply@yourdomain.com
MAILJET_SENDER_NAME=AcademOra

# ---- Stripe (Billing) ----
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
# Price IDs per plan (from Stripe Dashboard)
STRIPE_PRICE_FREE=
STRIPE_PRICE_PRO=
STRIPE_PRICE_TEAM=

# ---- Sentry (Monitoring) ----
SENTRY_DSN=
SENTRY_ENV=development

# ---- Rate limiting ----
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX=100

# ---- CORS ----
CORS_ORIGINS=http://localhost:5173# Server Configuration
PORT=3001
JWT_SECRET=your-secret-key-change-in-production

# Frontend API URL
VITE_API_URL=http://localhost:3001/api

# Supabase Configuration
# Get these from: https://supabase.com/dashboard -> Your Project -> Settings -> API
SUPABASE_URL=https://snflmjoiarpvtvqoawvz.supabase.co
SUPABASE_KEY=your-service-role-key-here
# OR use SUPABASE_SERVICE_ROLE_KEY instead of SUPABASE_KEY
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Mailjet Configuration
# Get these from: https://app.mailjet.com/account/api_keys
MAILJET_API_KEY=your-mailjet-api-key-hore
MAILJET_API_SECRET=your-mailjet-api-secret-hore

# Mailjet Sender Configuration (optional - defaults to values below)
MAILJET_SENDER_EMAIL=ay.perso2011@gmail.com
MAILJET_SENDER_NAME=AcademOra

# Frontend URL (for password reset links)
FRONTEND_URL=http://localhost:5173
